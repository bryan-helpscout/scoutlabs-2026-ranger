import AskChat from "@/app/components/AskChat";

export const metadata = {
  title: "Ask Ranger — Help Scout",
  description: "Ask Ranger any sales question — pricing, features, competitors, roadmap.",
};

export default function AskPage() {
  return <AskChat />;
}
